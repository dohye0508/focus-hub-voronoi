# 전국 청소년 이동형 쉼터 최적 입지 선정
## 공공데이터 AI 활용 분석 기획안

**프로젝트명**: 보로노이 다이어그램과 다기준 공간 최적화를 활용한  
　　　　　전국 청소년 이동형 쉼터 입지 선정 모델 개발  
**분야**: 공간 데이터 분석 / 사회복지 / 청소년 정책  
**작성일**: 2026년 5월

---

## 1. 문제 정의 (Problem Statement)

### 1.1 배경

대한민국의 사교육 시장은 세계적인 규모를 자랑하며, 2026년 5월 현재 전국적으로 **138,275개**의 학원 및 교습소가 등록되어 있다. 특히 특정 지역(서울 강남, 대구 수성, 부산 해운대 등)으로의 청소년 유동인구 쏠림 현상은 매우 심각하다.

학원 밀집 지역의 청소년들은 평균 하루 3~5시간 이상을 학원가에서 보내지만, 심리적 안정을 취하거나 긴급한 도움을 받을 수 있는 청소년 쉼터는 인천 전역에 단 **8개소**에 불과하다. 이는 학원 1개당 약 7,810명의 청소년이 공유하는 실정이다.

### 1.2 핵심 문제

1. **공간적 불균형**: 학원 밀집 지역과 쉼터 위치 간의 지리적 괴리가 크다.
2. **정적 인프라의 한계**: 고정형 쉼터는 유동 인구가 많은 학원가를 커버하지 못한다.
3. **데이터 기반 의사결정 부재**: 현재 쉼터 위치는 수요 데이터 분석 없이 설치된 경우가 많다.

### 1.3 제안

> "학원 밀집 데이터, 청소년 인구 통계, 스트레스 지표를 통합하여  
> 수학적 최적화 알고리즘으로 **이동형 쉼터 버스**의 최적 배치 위치를 도출한다."

---

## 2. 데이터 현황

### 2.1 보유 데이터셋

| 데이터명 | 행 수 | 출처 | 활용 목적 |
|---------|------|------|---------|
| 인천시 학원 등록 현황 | 62,476건 | 나이스(NICE) 교육부 공공데이터 | 수요점 (demand points) |
| 전국 청소년 쉼터 현황 (SHP) | 135개소 | 여성가족부 | 기존 시설 위치 |
| 시군구별 스트레스 인지율 | 10개 구 | 통계청 지역사회건강조사 (2025) | CVI 위험도 지표 |
| 주민등록 인구 통계 | 10개 구 | 행정안전부 (2026.04) | CVI 규모 지표 |
| 인천시 버스 정류장 이용 현황 | 6,757개소 | 인천광역시 공공데이터포털 | 후보 배치 지점 |
| 쉼터 컬럼 정의서 | - | 여성가족부 | 데이터 해석 참고 |

### 2.2 데이터 전처리

**학원 데이터 통합**
- 10개 군·구별 Excel 파일을 pandas로 병합
- 헤더 행 자동 탐지 (`'학원명'` 문자열 검색 후 컬럼 설정)
- 결측 주소 제거, 지역 컬럼 추가
- 최종 62,476행의 `academies_merged.csv` 생성

**청소년 쉼터 공간 데이터**
- EPSG:5186 (Korea 2000 / Central Belt) → EPSG:4326 (WGS84) 좌표계 변환
- 인천 소재 필터링: `A6 == '인천'` → 8개소 추출

---

## 3. 분석 방법론

### 3.1 보로노이 다이어그램 (Voronoi Diagram)

**정의**

학원 위치 집합 $S = \{s_1, s_2, \ldots, s_n\}$ 에 대해 각 학원 $s_i$의 보로노이 세력권은:

$$V(s_i) = \{ x \in \mathbb{R}^2 \mid d(x, s_i) \leq d(x, s_j),\ \forall j \neq i \}$$

여기서 $d(\cdot, \cdot)$는 유클리드 거리 함수이다.

**알고리즘**: Fortune's Algorithm (시간복잡도 $O(n \log n)$)  
**구현**: `scipy.spatial.Voronoi`

**색상 인코딩**

각 다각형의 HSL 색상은 지역 밀집도 순위에 따라 결정된다:

$$\text{hue}(i) = \left(180 + \frac{\text{rank}(i)}{N} \times 300\right) \mod 360$$

$$\text{saturation}(i) = 0.72 + \delta_n(i) \times 0.20$$

$$\text{lightness}(i) = 0.52 - \delta_n(i) \times 0.10$$

$$\delta_n(i) = \frac{d_i - d_{\min}}{d_{\max} - d_{\min}}$$

여기서 $d_i$는 해당 다각형 중심점 반경 0.025° 내 학원 수.

**해석**

- 다각형이 좁고 촘촘한 지역 → 학원 밀집, 청소년 수요 집중
- 다각형이 넓은 지역 → 공급 대비 수요가 분산됨
- 쉼터 마커가 넓은 다각형 안에 있다면 → 서비스 공백 (복지 사각지대)

---

### 3.2 커널 밀도 추정 (KDE, Kernel Density Estimation)

학원 위치를 점 과정으로 보고 연속 밀도 함수를 추정한다:

$$\hat{f}(x) = \frac{1}{nh} \sum_{i=1}^{n} K\!\left(\frac{x - x_i}{h}\right)$$

가우시안 커널을 사용할 경우:

$$K(u) = \frac{1}{\sqrt{2\pi}} e^{-\frac{u^2}{2}}$$

**파라미터 설정**

| 파라미터 | 값 | 의미 |
|---------|---|------|
| radius | 20 | 각 점의 영향 반경 (픽셀 단위) |
| blur | 16 | 가우시안 대역폭 $h$ 근사값 |
| min_opacity | 0.20 | 저밀도 지역 가시성 하한 |

**활용**: 보로노이가 경계를 보여준다면, KDE는 밀도의 '강도'를 연속면으로 표현한다. 두 시각화를 함께 보면 경계와 강도를 동시에 파악할 수 있다.

---

### 3.3 복합취약지수 (CVI, Composite Vulnerability Index)

단일 지표의 한계를 극복하고 다기준 의사결정(MCDM)을 구현하기 위해 CVI를 정의한다.

**정규화**

각 지표를 Min-Max 정규화로 [0, 1] 범위로 변환:

$$\tilde{X}_k(d) = \frac{X_k(d) - \min_d X_k}{\max_d X_k - \min_d X_k}$$

**CVI 계산식**

$$\text{CVI}(d) = w_A \cdot \tilde{A}(d) + w_S \cdot \tilde{S}(d) + w_P \cdot \tilde{P}(d)$$

| 기호 | 변수 | 가중치 $w$ | 데이터 출처 |
|-----|-----|-----------|-----------|
| $A(d)$ | 구 $d$의 학원 수 | $w_A = 0.40$ | NICE |
| $S(d)$ | 스트레스 인지율 (%) | $w_S = 0.30$ | 통계청 |
| $P(d)$ | 청소년 인구 (10~24세) | $w_P = 0.30$ | 행정안전부 |

제약: $w_A + w_S + w_P = 1$

**가중치 설정 근거**

- $w_A > w_S = w_P$: 학원 수가 수요의 직접 지표이므로 가장 높게 설정
- 스트레스와 청소년 인구는 위험도 및 규모를 동등하게 반영

**CVI 해석 기준**

| 구간 | 등급 | 정책 우선순위 |
|-----|-----|------------|
| CVI ≥ 0.70 | 최우선 | 이동형 쉼터 즉시 배치 |
| 0.50 ≤ CVI < 0.70 | 우선 | 단기 배치 계획 수립 |
| CVI < 0.50 | 보통 | 중장기 모니터링 |

---

### 3.4 p-median 최적화 (Location-Allocation Model)

**문제 정의**

$p$개의 이동형 쉼터를 배치하여 모든 학원생의 총 이동 거리를 최소화하는 최적 위치 $F$를 구한다:

$$\min_{F \subseteq X,\, |F|=p} \sum_{i \in D} w_i \cdot \min_{j \in F} d(i, j)$$

- $D$: 학원 위치 집합 (수요점)
- $F$: 쉼터 배치 후보 위치 집합 (시설점)
- $p$: 쉼터 수 (본 프로젝트에서 $p = 3$)
- $w_i$: 수요점 $i$의 가중치 (균등 설정)
- $d(i,j)$: 수요점 $i$와 시설점 $j$ 간의 유클리드 거리

**근사 해법: k-means 클러스터링**

p-median 문제는 NP-hard이므로 k-means 클러스터링으로 근사 해를 구한다:

1. 학원 위치 좌표 $\{(x_i, y_i)\}$를 $k=3$ 클러스터로 분류
2. 각 클러스터의 중심점(centroid) $\bar{c}_k = \frac{1}{|C_k|}\sum_{i \in C_k}(x_i, y_i)$
3. $\bar{c}_k$를 이동형 쉼터 배치 권장 위치로 제안

**구현**: `scipy.cluster.vq.kmeans2(points, 3, iter=30, seed=42)`

**결과 해석**: 도출된 3개 지점에 이동형 쉼터 버스를 배치하면 인천시 전체 학원생의 평균 접근 거리가 이론적으로 최소화된다.

---

## 4. 구현 내용 (What We Did)

### 4.1 데이터 파이프라인

```
[수집] NICE/행안부/통계청/여성가족부 공공데이터
   ↓
[전처리] 인코딩 처리 (cp949/utf-8), 헤더 탐지, 결측값 제거
   ↓
[통합] 10개 구 학원 파일 → academies_merged.csv (62,476행)
   ↓
[시뮬레이션] 구별 실제 학원 수 비례 → 600개 좌표 생성
   ↓
[분석] Voronoi + KDE + CVI + p-median
   ↓
[시각화] folium 인터랙티브 지도 → final_analysis.html
```

### 4.2 시각화 레이어 구성

| 레이어 | 기본 표시 | 설명 |
|-------|---------|------|
| 학원 세력권 (보로노이) | ✅ ON | 606개 다각형, 밀집도별 색상 |
| 열지도 (KDE) | ❌ OFF | 선택 오버레이 |
| 청소년 쉼터 | ✅ ON | 빨간 원형 마커 |
| 이동형 쉼터 권장지 | ✅ ON | 주황 별★ 마커 3개 |

### 4.3 대시보드 UI

- **KPI 4개 카드**: 등록학원 / 현재쉼터 / 배치권장지 / 분석영역 수
- **CVI 상위 3개 구 배지**: 즉각적인 취약 지역 식별
- **구별 비교 테이블**: 학원수 / 스트레스 / 청소년인구 / CVI 바 차트

---

## 5. 분석 결과 요약 (Expected/Actual Results)

### 5.1 보로노이 분석

- 연수구 (송도국제도시 일대)와 부평구에서 다각형이 가장 조밀하게 밀집
- 강화군·옹진군은 다각형 면적이 넓어 학원 밀도가 낮음
- 기존 쉼터 8개소 모두 학원 세력권 중심에서 벗어난 위치에 분포

### 5.2 CVI 결과 (예측값)

| 순위 | 구 | 학원수 | 스트레스 | 청소년인구 | CVI |
|----|---|------|--------|--------|-----|
| 1 | 연수구 | 14,612 | 23.1% | 76,233 | 높음 |
| 2 | 서구 | 13,984 | 24.9% | 116,849 | 높음 |
| 3 | 부평구 | 9,212 | 22.8% | 81,681 | 중상 |

### 5.3 p-median 권장 입지

| 입지 | 위치 권역 | 이유 |
|-----|---------|-----|
| 권장지 A | 연수구/남동구 경계 | 학원 최밀집 클러스터 중심 |
| 권장지 B | 부평구 중심 | 2번째 밀집 클러스터 |
| 권장지 C | 서구/계양구 권역 | 북부 수요 커버 |

---

## 6. 향후 계획 (Future Work)

### 6.1 단기 (발표 전)

- [x] 보로노이 다이어그램 생성 및 시각화
- [x] KDE 열지도 구현
- [x] CVI 계산 및 대시보드 반영
- [x] p-median 최적 입지 3개소 도출
- [ ] 구별 CVI 등급별 색상 구분 지도 (코로플레스)
- [ ] Moran's I 공간 자기상관 검정 결과 추가

### 6.2 중기 (발표 이후 개선)

- **카카오/네이버 지오코딩 API 연동**: 62,476개 실제 주소 좌표 변환
- **OSMnx 도로망 분석**: 유클리드 거리 → 실제 도보/버스 이동 거리
- **GWR (지리적 가중 회귀)**: 지역별 학원 수-스트레스 관계의 공간적 변동성 분석
- **시계열 분석**: 학원 증가 추세를 반영한 미래 수요 예측

### 6.3 중장기 정책 제안

- 인천시 청소년 정책과에 이동형 쉼터 버스 시범 운행 노선 제안
- 학원 밀집 시간대 (18:00~22:00) 집중 배치 운영 스케줄 제안
- 연간 모니터링을 통한 수요 변화 대응 체계 구축

---

## 7. 기술 스택

| 범주 | 도구 |
|-----|-----|
| 언어 | Python 3.12 |
| 공간 분석 | geopandas, shapely, pyproj |
| 수치 계산 | numpy, scipy (Voronoi, kmeans2) |
| 시각화 | folium, matplotlib |
| 데이터 처리 | pandas, openpyxl |
| 버전 관리 | Git / GitHub (dohye0508/focus-hub-voronoi) |

---

## 8. 프로젝트 의의

이 프로젝트는 단순히 "학원이 많은 곳에 쉼터를 놓자"는 직관적 접근을 넘어, 다음을 수학적으로 증명한다:

> **"어떤 지역의 청소년이 가장 많은 위험에 노출되어 있으며 (CVI),  
> 그 수요를 최소 비용으로 충족시킬 수 있는 위치는 어디인가 (p-median)"**

공공데이터만을 활용하여 청소년 복지 정책 의사결정을 지원할 수 있는 **재현 가능하고 확장 가능한 분석 프레임워크**를 제시한다.
